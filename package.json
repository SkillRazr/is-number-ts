{
  "name": "is-number-ts",
  "version": "0.1.1",
  "description": "isNumber with types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "rm -r ./dist && tsc && jasmine --config=src/spec/support/jasmine.json"
  },
  "files": [
    "/dist"
  ],
  "keywords": [
    "isNumber"
  ],
  "author": "SkillRazr",
  "license": "ISC",
  "devDependencies": {
    "@types/jasmine": "^4.3.0",
    "jasmine": "^4.4.0",
    "jasmine-spec-reporter": "^7.0.0"
  }
}
